{
  "$defs": {
    "AgentBackendConfig": {
      "additionalProperties": false,
      "properties": {
        "provider": {
          "const": "hermes",
          "default": "hermes",
          "description": "Agent backend provider. Config currently accepts only hermes.",
          "title": "Provider",
          "type": "string"
        },
        "config_scope": {
          "default": "isolated",
          "description": "Whether agent CLI calls load user-global configuration or run isolated from it.",
          "enum": [
            "isolated",
            "native"
          ],
          "title": "Config Scope",
          "type": "string"
        },
        "auto_context": {
          "default": "disabled",
          "description": "Whether agent CLI calls auto-load rules, memory, and implicit skill context.",
          "enum": [
            "disabled",
            "enabled"
          ],
          "title": "Auto Context",
          "type": "string"
        },
        "explicit_context": {
          "$ref": "#/$defs/ExplicitAgentContextConfig",
          "description": "Context explicitly injected by feishu-shadow-agent instead of discovered from user-global state."
        },
        "hermes": {
          "$ref": "#/$defs/HermesConfig",
          "description": "Hermes backend settings."
        }
      },
      "title": "AgentBackendConfig",
      "type": "object"
    },
    "ChatPolicyConfig": {
      "additionalProperties": false,
      "properties": {
        "name": {
          "default": "",
          "description": "Human-readable chat name for operators; not used as an identifier.",
          "title": "Name",
          "type": "string"
        },
        "auto_reply": {
          "default": false,
          "description": "Whether this chat may auto-reply when all gates pass.",
          "title": "Auto Reply",
          "type": "boolean"
        },
        "bot_joined": {
          "default": false,
          "description": "Whether the bot has joined this chat and can be used for bot replies and resource access.",
          "title": "Bot Joined",
          "type": "boolean"
        },
        "reply_identity": {
          "default": "bot_preferred",
          "description": "Reply identity for this chat: prefer bot with fallback, require bot, or send as user.",
          "enum": [
            "bot_preferred",
            "bot",
            "user"
          ],
          "title": "Reply Identity",
          "type": "string"
        },
        "allow_user_fallback": {
          "default": true,
          "description": "Whether group replies may fall back to user identity when bot_preferred cannot use the bot.",
          "title": "Allow User Fallback",
          "type": "boolean"
        },
        "resource_download": {
          "default": true,
          "description": "Whether downloadable message resources may be saved for this chat.",
          "title": "Resource Download",
          "type": "boolean"
        }
      },
      "title": "ChatPolicyConfig",
      "type": "object"
    },
    "DaemonConfig": {
      "additionalProperties": false,
      "properties": {
        "tick_interval_seconds": {
          "default": 60,
          "description": "Seconds between daemon polling ticks.",
          "exclusiveMinimum": 0,
          "title": "Tick Interval Seconds",
          "type": "integer"
        },
        "overlap_seconds": {
          "default": 120,
          "description": "Look-back window added to message fetches so delayed Feishu results are not missed.",
          "minimum": 0,
          "title": "Overlap Seconds",
          "type": "integer"
        }
      },
      "title": "DaemonConfig",
      "type": "object"
    },
    "DebugConfig": {
      "additionalProperties": false,
      "properties": {
        "save_full_agent_io": {
          "default": false,
          "description": "Whether to persist full agent prompts and outputs for debugging; keep false for normal operation.",
          "title": "Save Full Agent Io",
          "type": "boolean"
        }
      },
      "title": "DebugConfig",
      "type": "object"
    },
    "ExplicitAgentContextConfig": {
      "additionalProperties": false,
      "properties": {
        "skills": {
          "description": "Explicit skill directory paths or SKILL.md file paths to pass only to task-session agent turns.",
          "items": {
            "type": "string"
          },
          "title": "Skills",
          "type": "array"
        }
      },
      "title": "ExplicitAgentContextConfig",
      "type": "object"
    },
    "HealthConfig": {
      "additionalProperties": false,
      "properties": {
        "interval_seconds": {
          "default": 300,
          "description": "Seconds between full runtime health refreshes.",
          "exclusiveMinimum": 0,
          "title": "Interval Seconds",
          "type": "integer"
        },
        "retry_interval_seconds": {
          "default": 60,
          "description": "Seconds to wait before retrying runtime health after a critical failure.",
          "exclusiveMinimum": 0,
          "title": "Retry Interval Seconds",
          "type": "integer"
        },
        "timeout_seconds": {
          "default": 10,
          "description": "Default timeout in seconds for health probes.",
          "exclusiveMinimum": 0,
          "title": "Timeout Seconds",
          "type": "integer"
        }
      },
      "title": "HealthConfig",
      "type": "object"
    },
    "HermesConfig": {
      "additionalProperties": false,
      "properties": {
        "mode": {
          "default": "cli",
          "description": "Hermes health mode. Runtime task processing always uses the local Hermes CLI; 'http' adds a health URL check.",
          "enum": [
            "cli",
            "http"
          ],
          "title": "Mode",
          "type": "string"
        },
        "path": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional Hermes executable path for cli mode; null uses the current PATH.",
          "title": "Path"
        },
        "source": {
          "default": "feishu-shadow-agent",
          "description": "Source label passed to Hermes sessions and audit data.",
          "title": "Source",
          "type": "string"
        },
        "router_max_turns": {
          "default": 4,
          "description": "Maximum Hermes turns for task routing calls.",
          "exclusiveMinimum": 0,
          "title": "Router Max Turns",
          "type": "integer"
        },
        "session_max_turns": {
          "default": 8,
          "description": "Maximum Hermes turns for task session calls.",
          "exclusiveMinimum": 0,
          "title": "Session Max Turns",
          "type": "integer"
        },
        "model": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional Hermes model override; null uses the Hermes default.",
          "title": "Model"
        },
        "provider": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional Hermes provider override; null uses the Hermes default.",
          "title": "Provider"
        },
        "timeout_seconds": {
          "default": 60,
          "description": "Timeout in seconds for Hermes subprocess or health calls.",
          "exclusiveMinimum": 0,
          "title": "Timeout Seconds",
          "type": "integer"
        },
        "health_url": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Additional HTTP health URL used when agent_backend.hermes.mode is 'http'; must start with http:// or https://.",
          "title": "Health Url"
        },
        "api_key_env": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": "HERMES_API_KEY",
          "description": "Environment variable name that holds the Hermes API key for agent_backend.hermes.mode http; the secret value is never stored here.",
          "title": "Api Key Env"
        }
      },
      "title": "HermesConfig",
      "type": "object"
    },
    "LarkCliConfig": {
      "additionalProperties": false,
      "properties": {
        "path": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional lark-cli executable path; null uses the current PATH.",
          "title": "Path"
        },
        "timeout_seconds": {
          "default": 30,
          "description": "Timeout in seconds for lark-cli subprocess calls.",
          "exclusiveMinimum": 0,
          "title": "Timeout Seconds",
          "type": "integer"
        }
      },
      "title": "LarkCliConfig",
      "type": "object"
    },
    "LifecycleConfig": {
      "additionalProperties": false,
      "properties": {
        "watch_minutes": {
          "default": 120,
          "description": "Minutes to keep watching a task after activity.",
          "exclusiveMinimum": 0,
          "title": "Watch Minutes",
          "type": "integer"
        },
        "closed_recall_days": {
          "default": 7,
          "description": "Days to consider closed tasks for explicit recall.",
          "minimum": 1,
          "title": "Closed Recall Days",
          "type": "integer"
        },
        "approval_timeout_hours": {
          "anyOf": [
            {
              "minimum": 1,
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": 24,
          "description": "Hours before pending approvals expire; null means approvals never expire.",
          "title": "Approval Timeout Hours"
        }
      },
      "title": "LifecycleConfig",
      "type": "object"
    },
    "LoggingConfig": {
      "additionalProperties": false,
      "properties": {
        "jsonl_path": {
          "default": "logs/agent.jsonl",
          "description": "JSONL log path, resolved relative to the config file when not absolute.",
          "title": "Jsonl Path",
          "type": "string"
        },
        "level": {
          "default": "info",
          "description": "Minimum runtime log level written to configured sinks.",
          "enum": [
            "debug",
            "info",
            "warning",
            "error"
          ],
          "title": "Level",
          "type": "string"
        },
        "console": {
          "default": false,
          "description": "Whether to also write human-readable runtime logs to stderr.",
          "title": "Console",
          "type": "boolean"
        },
        "text_path": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional human-readable log file path, resolved relative to the config file when not absolute.",
          "title": "Text Path"
        }
      },
      "title": "LoggingConfig",
      "type": "object"
    },
    "OwnerConfig": {
      "additionalProperties": false,
      "properties": {
        "open_id": {
          "description": "Feishu open_id of the single owner who receives approval notifications and can run local commands.",
          "minLength": 1,
          "title": "Open Id",
          "type": "string"
        },
        "name": {
          "default": "",
          "description": "Optional display name for humans reading config and logs.",
          "title": "Name",
          "type": "string"
        }
      },
      "required": [
        "open_id"
      ],
      "title": "OwnerConfig",
      "type": "object"
    },
    "ReplyPolicyConfig": {
      "additionalProperties": false,
      "properties": {
        "p2p_auto_reply": {
          "default": true,
          "description": "Whether one-to-one chats may auto-reply when reply gates pass.",
          "title": "P2P Auto Reply",
          "type": "boolean"
        },
        "unknown_group_auto_reply": {
          "default": false,
          "description": "Whether groups without an explicit chats entry may auto-reply when all gates pass.",
          "title": "Unknown Group Auto Reply",
          "type": "boolean"
        }
      },
      "title": "ReplyPolicyConfig",
      "type": "object"
    },
    "RetentionConfig": {
      "additionalProperties": false,
      "properties": {
        "raw_message_days": {
          "default": 30,
          "description": "Days to retain raw message payloads.",
          "minimum": 1,
          "title": "Raw Message Days",
          "type": "integer"
        },
        "resource_days": {
          "default": 30,
          "description": "Days to retain downloaded resource files.",
          "minimum": 1,
          "title": "Resource Days",
          "type": "integer"
        }
      },
      "title": "RetentionConfig",
      "type": "object"
    },
    "StorageConfig": {
      "additionalProperties": false,
      "properties": {
        "sqlite_path": {
          "default": "data/agent.sqlite3",
          "description": "SQLite database path, resolved relative to the config file when not absolute.",
          "title": "Sqlite Path",
          "type": "string"
        },
        "resource_dir": {
          "default": "data/resources",
          "description": "Safe relative directory for downloaded message resources; absolute paths and '..' are rejected.",
          "title": "Resource Dir",
          "type": "string"
        }
      },
      "title": "StorageConfig",
      "type": "object"
    }
  },
  "additionalProperties": false,
  "properties": {
    "owner": {
      "$ref": "#/$defs/OwnerConfig",
      "description": "Single owner identity used for approvals and operator notifications."
    },
    "daemon": {
      "$ref": "#/$defs/DaemonConfig",
      "description": "Daemon polling and overlap settings."
    },
    "health": {
      "$ref": "#/$defs/HealthConfig",
      "description": "Runtime health refresh and timeout settings."
    },
    "storage": {
      "$ref": "#/$defs/StorageConfig",
      "description": "Local SQLite and resource storage settings."
    },
    "logging": {
      "$ref": "#/$defs/LoggingConfig",
      "description": "Local structured logging settings."
    },
    "lark_cli": {
      "$ref": "#/$defs/LarkCliConfig",
      "description": "lark-cli executable and timeout settings."
    },
    "agent_backend": {
      "$ref": "#/$defs/AgentBackendConfig",
      "description": "Agent backend provider, isolation policy, explicit context, and provider-specific settings."
    },
    "reply_policy": {
      "$ref": "#/$defs/ReplyPolicyConfig",
      "description": "Global auto-reply policy used before chat-specific overrides."
    },
    "chats": {
      "additionalProperties": {
        "$ref": "#/$defs/ChatPolicyConfig"
      },
      "description": "Per-chat policy overrides keyed by Feishu chat_id, such as oc_xxx.",
      "title": "Chats",
      "type": "object"
    },
    "tool_permissions": {
      "default": "guarded_write",
      "description": "Agent backend tool permission profile: read_only, guarded_write, or full_access.",
      "enum": [
        "read_only",
        "guarded_write",
        "full_access"
      ],
      "title": "Tool Permissions",
      "type": "string"
    },
    "retention": {
      "$ref": "#/$defs/RetentionConfig",
      "description": "Local data retention settings."
    },
    "lifecycle": {
      "$ref": "#/$defs/LifecycleConfig",
      "description": "Global task lifecycle settings."
    },
    "debug": {
      "$ref": "#/$defs/DebugConfig",
      "description": "Debug-only persistence settings."
    }
  },
  "required": [
    "owner"
  ],
  "title": "AppConfig",
  "type": "object"
}
